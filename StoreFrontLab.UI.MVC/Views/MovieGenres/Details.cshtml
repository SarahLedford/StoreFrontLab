@model StoreFrontLab.DATA.EF.MovieGenre

@{
    ViewBag.Title = "Details";
}

<h2>@ViewBag.Title</h2>

<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.MovieGenreName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.MovieGenreName)
        </dd>

    </dl>
</div>
<p>
    @if (User.IsInRole("Admin") || User.IsInRole("Employee"))
    {
        @:@Html.ActionLink("Edit", "Edit", new { id = Model.MovieGenreID }) |

    }
    @Html.ActionLink("Back to Movie Genres", "Index")
</p>
