@model StoreFrontLab.DATA.EF.MovieGenre

@{
    ViewBag.Title = "Delete "+Model.MovieGenreName;
}

<h2>@ViewBag.Title</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.MovieGenreName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.MovieGenreName)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete Permanently" class="btn btn-danger float-left" />
            @Html.ActionLink("Back to Movie Genres", "Index", null, new { @class="btn btn-info float-right" })
        </div>
    }
</div>
