@model StoreFrontLab.DATA.EF.MusicGenre

@{
    ViewBag.Title = "Delete "+Model.MusicGenreName;
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.MusicGenreName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.MusicGenreName)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-danger float-left" />
            @Html.ActionLink("Back to Music Genres", "Index", null, new { @class="btn btn-info float-right"})
        </div>
    }
</div>
